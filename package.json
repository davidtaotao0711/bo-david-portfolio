{
  "name": "bo-david-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run images && astro dev --host 127.0.0.1",
    "build": "npm run images && astro build",
    "preview": "astro preview --host 127.0.0.1",
    "typecheck": "astro check",
    "images": "tsx scripts/images.ts",
    "test": "tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^24.0.0",
    "sharp": "^0.35.4",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
